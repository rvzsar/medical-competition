# Настройка Redis для постоянного хранения данных

## 🎯 Проблема

При развертывании на Vercel данные, хранящиеся в памяти сервера, теряются при каждом обновлении страницы или redeploy. Для решения этой проблемы мы используем Redis - постоянное хранилище данных.

## 📋 Что нужно сделать

### 1. Создание Redis базы данных

У вас уже есть Redis база данных на Vercel! Просто убедитесь, что environment variable `REDIS_URL` настроена правильно:

```
REDIS_URL="redis://default:1111111@redis-15772.c239.us-east-1-2.ec2.redns.redis-cloud.com:15772"
```

### 2. Проверка Environment Variables

В Vercel Dashboard:
1. Перейдите в ваш проект
2. Нажмите **"Settings"** → **"Environment Variables"**
3. Убедитесь, что `REDIS_URL` добавлена со значением вашей Redis базы данных

### 3. Проверка работы

После развертывания проекта с Vercel KV:

1. Добавьте команду через админ-панель
2. Обновите страницу
3. Команда должна остаться в списке

## 🔧 Техническая реализация

### Установленные пакеты
```bash
npm install redis
```

### Структура хранения данных

```
medical-competition:teams           - Список команд
medical-competition:team-scores     - Оценки команд
medical-competition:aggregated-scores - Агрегированные оценки
medical-competition:jury-members    - Члены жюри
medical-competition:backup:*        - Резервные копии
```

### Ключевые функции

- `getAllData()` - получение всех данных
- `addTeam()` - добавление команды
- `addTeamScore()` - добавление оценки
- `updateTeam()` - обновление команды
- `deleteTeam()` - удаление команды
- `backupData()` - создание резервной копии
- `restoreData()` - восстановление из копии

## 🚀 Преимущества Redis на Vercel

1. **Постоянное хранение** - данные не теряются при redeploy
2. **Высокая производительность** - Redis работает очень быстро
3. **Гибкость** - работает с любыми Redis-совместимыми клиентами
4. **Надежность** - проверенная технология с годами использования
5. **Интеграция с Vercel** - легко настраивается через environment variables

## 📊 Лимиты и рекомендации

- Зависит от вашего Redis провайдера
- Обычно хватает для медицинских олимпиад
- Рекомендуется мониторить использование в Vercel Dashboard

## 🔒 Безопасность

- Все данные передаются по защищенному соединению Redis
- Аутентификация через environment variables
- Изоляция данных между проектами
- Регулярное резервное копирование данных

## 🛠️ Отладка

Если данные не сохраняются:

1. Проверьте, что `REDIS_URL` правильно настроена в Environment Variables
2. Убедитесь, что Redis база данных доступна
3. Проверьте логи в Vercel Dashboard на предмет ошибок подключения
4. Убедитесь, что проект переразвернут после добавления `REDIS_URL`

## 📝 Альтернативы

Если Redis по какой-то причине не подходит:

1. **Vercel KV** - специализированное решение для Vercel
2. **Vercel Postgres** - для сложных запросов
3. **Supabase** - внешняя база данных
4. **PlanetScale** - MySQL совместимая база
5. **MongoDB Atlas** - NoSQL решение

Но для данной задачи Redis является оптимальным решением.

---

**После настройки Redis все данные будут сохраняться постоянно!**

## 🎯 Готово к использованию!

Теперь ваша система:
- ✅ Сохраняет все данные в Redis
- ✅ Работает с несколькими пользователями одновременно
- ✅ Не теряет данные при обновлении страницы
- ✅ Готова к развертыванию на Vercel

Просто разверните проект и добавьте `REDIS_URL` в Environment Variables!