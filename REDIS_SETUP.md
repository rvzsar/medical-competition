# 🔧 Быстрая настройка Redis для проекта

## ⚡ Важно!

Проект использует **стандартный Redis client** с переменной `REDIS_URL`.

**Формат:** `redis://default:password@host:port`

---

## 📝 Шаг 1: Создание Redis в Upstash

1. Откройте [console.upstash.com](https://console.upstash.com)
2. Нажмите **"Create Database"**
3. Выберите:
   - **Name:** `medical-competition`
   - **Type:** Regional (бесплатно)
   - **Region:** Ближайший к вам регион
4. Нажмите **"Create"**

---

## 📋 Шаг 2: Получение REDIS_URL

### Вариант A: Копирование готового URL

В Upstash Console найдите раздел **"Redis Connect"** или **"Connect"**:

```
Endpoint: redis-12345.upstash.io:6379
Password: AaBbCcDd123456789
```

**Соберите URL:**
```bash
REDIS_URL=redis://default:AaBbCcDd123456789@redis-12345.upstash.io:6379
```

### Вариант B: Использование REST API (альтернатива)

Если вы видите только REST API credentials:

```
UPSTASH_REDIS_REST_URL=https://redis-12345.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxx
```

**Преобразуйте в Redis URL:**
1. Найдите пароль в разделе "Details" → "Password"
2. Соберите URL:
   ```bash
   REDIS_URL=redis://default:ВАШ_ПАРОЛЬ@redis-12345.upstash.io:6379
   ```

---

## 🔐 Шаг 3: Добавление в проект

### Локально (.env.local):

```bash
# Создайте файл .env.local в корне проекта
REDIS_URL=redis://default:your_password@your-host.upstash.io:6379

# Опционально для email
RESEND_API_KEY=re_xxxxx
EMAIL_FROM=noreply@yourdomain.com
```

### На Vercel:

1. Откройте проект в [vercel.com/dashboard](https://vercel.com/dashboard)
2. Settings → Environment Variables
3. Добавьте:
   - **Name:** `REDIS_URL`
   - **Value:** `redis://default:password@host:6379`
   - **Environments:** Production, Preview, Development

---

## ✅ Проверка подключения

### Тест локально:

```bash
cd medical-competition
npm run dev
```

Откройте `http://localhost:3000/admin/teams` и создайте тестовую команду.

**Если команда сохранилась - Redis работает! 🎉**

### Тест на Vercel:

После deployment:
1. Откройте ваш сайт
2. Создайте команду в `/admin/teams`
3. Обновите страницу - команда должна остаться

---

## 🆘 Частые проблемы

### ❌ "REDIS_URL environment variable is not set"

**Решение:**
- Проверьте, что `.env.local` существует
- Проверьте, что переменная называется именно `REDIS_URL` (не `KV_REST_API_URL`)
- Перезапустите сервер (`Ctrl+C` и `npm run dev`)

### ❌ "Connection refused" или "ECONNREFUSED"

**Решение:**
- Проверьте формат URL: `redis://default:password@host:6379`
- Проверьте, что порт `6379` указан
- Проверьте, что пароль правильный

### ❌ "Invalid password"

**Решение:**
- Скопируйте пароль из Upstash Console (раздел Details)
- Убедитесь, что нет лишних пробелов
- Пароль идет после `default:` в URL

### ❌ Данные не сохраняются после перезагрузки

**Решение:**
- Убедитесь, что `REDIS_URL` настроен правильно
- Проверьте логи: в терминале должно быть "Redis Client Connected"
- На Vercel проверьте Environment Variables

---

## 💡 Полезные команды

### Проверка переменных окружения:

```bash
# Локально
echo $REDIS_URL

# Или создайте тестовый файл
node -e "console.log(process.env.REDIS_URL)"
```

### Просмотр логов на Vercel:

1. Dashboard → Your Project
2. Deployments → Latest
3. Functions → View Logs

---

## 📊 Лимиты Upstash (бесплатный тарф)

- ✅ **10,000 команд/день** - достаточно для олимпиады
- ✅ **256 MB памяти** - хватит на тысячи команд
- ✅ **Автоматические backups**
- ✅ **TLS/SSL шифрование**

**Для типичной олимпиады (50-100 участников) - более чем достаточно!**

---

## 🎯 Быстрая шпаргалка

```bash
# 1. Создайте Redis в Upstash
# 2. Скопируйте credentials
# 3. Соберите URL:

REDIS_URL=redis://default:PASSWORD@HOST:6379

# 4. Добавьте в .env.local (локально)
# 5. Добавьте в Vercel Environment Variables (production)
# 6. Готово! 🎉
```

---

## 📚 Дополнительная информация

- **Полная документация:** [`DEPLOYMENT.md`](DEPLOYMENT.md)
- **Техническая проверка:** [`ТЕХНИЧЕСКАЯ_ПРОВЕРКА.md`](ТЕХНИЧЕСКАЯ_ПРОВЕРКА.md)
- **Основной README:** [`README.md`](README.md)

---

## 🤝 Нужна помощь?

1. Проверьте [DEPLOYMENT.md](DEPLOYMENT.md) - детальные инструкции
2. Проверьте [ТЕХНИЧЕСКАЯ_ПРОВЕРКА.md](ТЕХНИЧЕСКАЯ_ПРОВЕРКА.md) - troubleshooting
3. Проверьте логи в терминале или Vercel Dashboard

**Удачи с настройкой! 🚀**