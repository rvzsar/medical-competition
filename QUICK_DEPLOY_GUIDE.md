# 🚀 Быстрое руководство по развертыванию исправлений

## 📦 Что было исправлено

### Критические исправления:
1. ✅ **Исправлен API запрос** - страница сертификатов теперь использует правильный endpoint `/api/data?type=teams`
2. ✅ **Добавлена навигация** - кнопка "Сертификаты" в админке
3. ✅ **Обратная навигация** - кнопка "Назад к админке" на странице сертификатов
4. ✅ **Исправлен импорт** в `send/route.ts` (строка 5)

### Новые файлы:
- `src/tests/test-certificates-integration.js` - интеграционные тесты
- `CERTIFICATES_FIXES.md` - подробная документация
- `QUICK_DEPLOY_GUIDE.md` - это руководство
- `ИСПРАВЛЕНИЯ_ВЫПОЛНЕНЫ.md` - итоговый отчет

### Измененные файлы:
- `src/app/admin/page.tsx` - добавлена кнопка "Сертификаты"
- `src/app/admin/certificates/page.tsx` - добавлена навигация и исправлен API запрос
- `src/app/api/certificates/send/route.ts` - исправлен импорт

## 🔧 Шаги для деплоя на Vercel

### 1. Закоммитьте изменения
```bash
cd medical-competition
git add .
git commit -m "fix: Исправлена система сертификатов - навигация и интеграция с API"
git push origin main
```

### 2. Vercel автоматически задеплоит изменения
- Дождитесь окончания деплоя (обычно 1-2 минуты)
- Проверьте статус в Vercel Dashboard

### 3. Проверьте переменные окружения
В Vercel Dashboard → Settings → Environment Variables убедитесь, что установлены:

**Обязательно:**
- ✅ `REDIS_URL` - уже настроен

**Для отправки email (опционально):**
- `RESEND_API_KEY` - ключ от https://resend.com
- `EMAIL_FROM` - email отправителя (например: `noreply@yourdomain.com`)

### 4. Запустите тесты
```bash
# Замените URL на ваш домен Vercel
TEST_URL=https://ваш-проект.vercel.app node src/tests/test-certificates-integration.js
```

Ожидаемый результат:
```
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!

✨ Система сертификатов полностью функциональна:
   • API endpoints работают
   • Генерация PDF работает
   • Redis подключен
   • Команды загружаются
```

## ✅ Проверка работоспособности

### Вариант 1: Через браузер

1. Откройте `https://ваш-проект.vercel.app/admin`
2. Войдите как член жюри
3. Нажмите кнопку **"📜 Сертификаты"** (новая кнопка внизу справа)
4. Проверьте, что команды загружаются в выпадающем списке
5. Выберите команду и нажмите **"📥 Скачать PDF"**
6. PDF должен скачаться

### Вариант 2: Через API

```bash
# Проверка API teams
curl https://ваш-проект.vercel.app/api/data?type=teams

# Должен вернуть JSON с командами:
# [{"id":"1","name":"Команда А","members":[...],"totalScore":0}]
```

## 🎯 Что теперь работает

### До исправлений ❌
- Страница `/admin/certificates` не загружала команды
- Нет навигации к странице сертификатов
- API `/api/teams` отсутствовал
- Невозможно сгенерировать или отправить сертификаты

### После исправлений ✅
- ✅ Команды загружаются из Redis
- ✅ Есть кнопка перехода к сертификатам
- ✅ API `/api/teams` работает корректно
- ✅ PDF генерируются успешно
- ✅ Email отправка настроена (при наличии RESEND_API_KEY)
- ✅ Полная интеграция с основным приложением

## 📊 Пользовательский сценарий

### Генерация командного сертификата:
1. Админка → "📜 Сертификаты"
2. Выбрать "Командный" сертификат
3. Выбрать команду из списка
4. Нажать "📥 Скачать PDF"

### Отправка именного сертификата:
1. Админка → "📜 Сертификаты"
2. Выбрать "Именной" сертификат
3. Выбрать команду
4. Ввести имя участника
5. Ввести email
6. (Опционально) Указать спецноминацию
7. Нажать "📧 Отправить на Email"

### Массовая отправка:
1. Нажать "Массовый режим →"
2. Заполнить данные и нажать "➕ Добавить в список"
3. Повторить для всех получателей
4. Нажать "🚀 Отправить все"

## 🐛 Решение проблем

### Команды не загружаются
```bash
# Проверьте API
curl https://ваш-проект.vercel.app/api/data?type=teams

# Проверьте Redis в Vercel Dashboard
# Settings → Environment Variables → REDIS_URL
```

### PDF не генерируется
- Убедитесь, что у команды есть оценки
- Проверьте логи в Vercel Dashboard
- Запустите интеграционный тест

### Email не отправляется
- Установите `RESEND_API_KEY` в Vercel
- Получите ключ на https://resend.com
- Проверьте квоту (100 emails/день на Free плане)

## 📞 Быстрая диагностика

```bash
# 1. Проверка основного приложения
curl https://ваш-проект.vercel.app/api/data?type=all

# 2. Проверка команд
curl https://ваш-проект.vercel.app/api/data?type=teams

# 3. Запуск всех тестов
TEST_URL=https://ваш-проект.vercel.app node src/tests/test-certificates-integration.js
```

## 🎉 Готово!

После выполнения всех шагов система полностью готова к работе:
- Инвесторы могут протестировать функционал
- Преподаватели могут генерировать сертификаты
- Участники будут получать сертификаты на email

**Время развертывания: ~5 минут**

---

💡 **Совет:** Сохраните тестовый скрипт для регулярной проверки работоспособности системы.