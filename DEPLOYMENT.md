# 🚀 Deployment Guide - Vercel

Пошаговая инструкция по развертыванию системы олимпиады на Vercel с настройкой всех необходимых сервисов.

## 📋 Что вам понадобится

- [ ] Аккаунт GitHub
- [ ] Аккаунт Vercel (бесплатный)
- [ ] Аккаунт Upstash (бесплатный)
- [ ] Аккаунт Resend (бесплатный, опционально)

## 🔧 Шаг 1: Подготовка проекта

### 1.1 Инициализация Git (если еще не сделано)

```bash
cd medical-competition
git init
git add .
git commit -m "Initial commit: Medical competition system"
```

### 1.2 Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Название: `medical-competition`
4. Приватность: на ваш выбор
5. НЕ создавайте README, .gitignore (уже есть)

```bash
git remote add origin https://github.com/YOUR_USERNAME/medical-competition.git
git branch -M main
git push -u origin main
```

## ☁️ Шаг 2: Настройка Upstash Redis

### 2.1 Создание базы данных

1. Регистрация: [console.upstash.com](https://console.upstash.com)
2. Нажмите "Create Database"
3. Параметры:
   - **Name**: `medical-competition`
   - **Type**: Regional (бесплатно)
   - **Region**: Ближайший к вашим пользователям
   - **Eviction**: No eviction (важно!)

### 2.2 Получение credentials

После создания БД:
1. Откройте вкладку "Details"
2. Найдите секцию "REST API"
3. Скопируйте **`UPSTASH_REDIS_REST_URL`** - это и будет ваш `REDIS_URL`
   
   **Важно**: Формат должен быть `redis://default:password@host:port`
   
   Если Upstash дал вам REST URL, преобразуйте его:
   - REST URL: `https://xxx.upstash.io`
   - Redis URL: `redis://default:YOUR_PASSWORD@xxx.upstash.io:6379`

**Сохраните эти значения!** Они понадобятся для Vercel.

## 📧 Шаг 3: Настройка Resend (для email)

### 3.1 Регистрация и получение API Key

1. Регистрация: [resend.com/signup](https://resend.com/signup)
2. После входа: API Keys → Create API Key
3. Название: `Medical Competition`
4. Permissions: Full Access
5. Скопируйте ключ (показывается один раз!)

**Сохраните API key!**

### 3.2 Настройка домена (для production)

**Для тестирования**: можно использовать `onboarding@resend.dev`

**Для production**:
1. В Resend: Domains → Add Domain
2. Введите ваш домен: `yourdomain.com`
3. Добавьте DNS записи (показаны в интерфейсе):
   - SPF запись
   - DKIM записи
   - DMARC запись (опционально)
4. Дождитесь верификации (обычно 5-10 минут)
5. EMAIL_FROM будет: `noreply@yourdomain.com`

**Альтернатива для теста**: используйте Resend Testing Domain
- EMAIL_FROM: `onboarding@resend.dev`
- Письма будут приходить только на ваш зарегистрированный email

## 🚀 Шаг 4: Deploy на Vercel

### 4.1 Подключение проекта

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New" → "Project"
4. Выберите репозиторий `medical-competition`
5. Нажмите "Import"

### 4.2 Настройка проекта

**Framework Preset**: Next.js (определится автоматически)

**Root Directory**: `./` (корень)

**Build Command**: `npm run build` (по умолчанию)

**Output Directory**: `.next` (по умолчанию)

### 4.3 Environment Variables

Нажмите "Environment Variables" и добавьте:

#### Обязательные (Redis):

```bash
REDIS_URL=redis://default:your_password@your-redis.upstash.io:6379
```

#### Опциональные (Email):

```bash
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com
```

**Важно**: Выберите для всех переменных:
- ✅ Production
- ✅ Preview
- ✅ Development

### 4.4 Deploy!

Нажмите **"Deploy"** и дождитесь завершения (2-3 минуты)

## ✅ Шаг 5: Проверка работоспособности

### 5.1 Основной функционал

После успешного деплоя:

1. **Откройте ваше приложение**: `https://your-app.vercel.app`

2. **Проверьте главную страницу**:
   - Должна отображаться информация об олимпиаде
   - Навигация работает

3. **Проверьте админ-панель команд**: `/admin/teams`
   - Создайте тестовую команду
   - Проверьте, что она сохранилась после перезагрузки страницы

4. **Проверьте жюри панель**: `/jury`
   - Выберите конкурс
   - Попробуйте выставить оценки

5. **Проверьте результаты**: `/results`
   - Должна отображаться таблица с командами и баллами

### 5.2 Система сертификатов

1. **Откройте панель сертификатов**: `/admin/certificates`

2. **Тест генерации PDF**:
   - Выберите команду
   - Нажмите "Скачать PDF"
   - Проверьте, что PDF скачался и корректно отображается

3. **Тест отправки email** (если настроили Resend):
   - Введите свой email
   - Нажмите "Отправить на Email"
   - Проверьте почту (включая спам)

## 🐛 Troubleshooting

### Ошибка: "REDIS_URL is not defined"

**Решение**:
1. Vercel Dashboard → Settings → Environment Variables
2. Проверьте, что переменные добавлены
3. Сделайте redeploy: Deployments → ... → Redeploy

### Ошибка: "Failed to connect to Upstash"

**Решение**:
1. Проверьте правильность URL и TOKEN
2. Убедитесь, что БД не удалена в Upstash
3. Проверьте статус Upstash: [status.upstash.com](https://status.upstash.com)

### Ошибка при генерации PDF

**Решение**:
1. Проверьте логи в Vercel: Deployments → Functions
2. Убедитесь, что `@react-pdf/renderer` установлен
3. Проверьте, что команда имеет результаты

### Email не отправляются

**Решение**:
1. Проверьте `RESEND_API_KEY` в Environment Variables
2. Убедитесь, что не превышен лимит (3,000/месяц)
3. Для production - проверьте верификацию домена
4. Проверьте логи Resend Dashboard

### Данные не сохраняются

**Решение**:
1. Проверьте, что используется Upstash KV, а не локальное хранилище
2. Проверьте Environment Variables
3. Убедитесь, что в Upstash нет eviction policy

## 🔐 Безопасность для Production

### Добавьте авторизацию

Создайте `middleware.ts` в корне `src/`:

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  // Защита админ-панелей
  if (request.nextUrl.pathname.startsWith('/admin')) {
    const authHeader = request.headers.get('authorization');
    
    if (!authHeader || !checkAuth(authHeader)) {
      return new NextResponse('Unauthorized', {
        status: 401,
        headers: {
          'WWW-Authenticate': 'Basic realm="Admin Area"',
        },
      });
    }
  }
  
  return NextResponse.next();
}

function checkAuth(authHeader: string): boolean {
  // Простая Basic Auth (для production используйте более надежное решение)
  const base64Credentials = authHeader.split(' ')[1];
  const credentials = Buffer.from(base64Credentials, 'base64').toString('ascii');
  const [username, password] = credentials.split(':');
  
  return username === process.env.ADMIN_USERNAME && 
         password === process.env.ADMIN_PASSWORD;
}

export const config = {
  matcher: '/admin/:path*',
};
```

Добавьте в Environment Variables:
```bash
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password
```

### Rate Limiting

Рассмотрите использование Vercel Edge Config или внешних сервисов для rate limiting.

## 📊 Мониторинг

### Vercel Analytics

1. Vercel Dashboard → Analytics
2. Включите Analytics (бесплатно)
3. Отслеживайте:
   - Количество посещений
   - Производительность
   - Ошибки

### Upstash Monitoring

1. Upstash Console → Database → Metrics
2. Отслеживайте:
   - Количество команд
   - Использование памяти
   - Latency

### Resend Monitoring

1. Resend Dashboard → Emails
2. Отслеживайте:
   - Отправленные письма
   - Доставляемость
   - Ошибки

## 🎉 Готово!

Ваше приложение успешно развернуто и готово к использованию!

### Полезные ссылки:

- **Production URL**: `https://your-app.vercel.app`
- **Admin Teams**: `https://your-app.vercel.app/admin/teams`
- **Certificates**: `https://your-app.vercel.app/admin/certificates`
- **Jury Panel**: `https://your-app.vercel.app/jury`
- **Results**: `https://your-app.vercel.app/results`

### Следующие шаги:

1. ✅ Протестируйте все функции
2. ✅ Создайте команды участников
3. ✅ Настройте доступ для жюри
4. ✅ Проведите тестовую оценку
5. ✅ Сгенерируйте тестовые сертификаты
6. ✅ Проверьте email-рассылку
7. ✅ Добавьте авторизацию для production
8. ✅ Настройте custom domain (опционально)

---

**Удачи с проведением олимпиады! 🏆**
