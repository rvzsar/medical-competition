# 📜 Система управления сертификатами

Полное руководство по настройке и использованию системы автоматической генерации и отправки сертификатов для олимпиады по акушерству и гинекологии.

## 🎯 Возможности

- ✅ Автоматическая генерация красивых PDF-сертификатов
- ✅ Два типа сертификатов: командные и именные
- ✅ Автоматическая отправка по email с красивым письмом
- ✅ Массовая отправка сертификатов
- ✅ Поддержка специальных номинаций
- ✅ Автоматический расчет мест и баллов
- ✅ Профессиональный дизайн с поддержкой кириллицы
- ✅ Удобная админ-панель

## 🚀 Быстрый старт

### 1. Установка зависимостей

Все необходимые пакеты уже установлены:
- `@react-pdf/renderer` - генерация PDF
- `resend` - отправка email
- `@react-email/components` - красивые email шаблоны

### 2. Настройка переменных окружения

Создайте файл `.env.local` на основе `.env.local.example`:

```bash
# Redis Configuration (Upstash или любой Redis) - уже настроено
REDIS_URL=redis://default:your_password@your-redis.upstash.io:6379

# Resend API Key - НУЖНО НАСТРОИТЬ
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxx

# Email Configuration
EMAIL_FROM=noreply@yourdomain.com
```

### 3. Получение Resend API Key (БЕСПЛАТНО)

1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. **Бесплатный план**: 100 email/день, 3,000 email/месяц
3. Перейдите в API Keys → Create API Key
4. Скопируйте ключ в `.env.local`

**Важно**: Для production нужно верифицировать домен в Resend.

### 4. Настройка домена для email (опционально)

**Для тестирования**: можно использовать `onboarding@resend.dev` (ограничено вашим email)

**Для production**:
1. В Resend: Domains → Add Domain
2. Добавьте DNS записи в настройки домена
3. Дождитесь верификации
4. Обновите `EMAIL_FROM` в `.env.local`

## 📖 Использование

### Доступ к админ-панели

Откройте в браузере:
```
http://localhost:3000/admin/certificates
```

На production:
```
https://your-app.vercel.app/admin/certificates
```

### Генерация командного сертификата

1. Выберите "Командный" тип
2. Выберите команду из списка
3. Введите email команды
4. (Опционально) Добавьте специальную номинацию
5. Нажмите:
   - **📥 Скачать PDF** - для локального сохранения
   - **📧 Отправить на Email** - для автоматической отправки

### Генерация именного сертификата

1. Выберите "Именной" тип
2. Выберите команду
3. Введите ФИО участника
4. Введите email участника
5. (Опционально) Добавьте специальную номинацию
6. Выберите действие

### Массовая отправка

1. Нажмите **"Массовый режим →"**
2. Для каждого получателя:
   - Заполните все поля
   - Нажмите **➕ Добавить в список**
3. Проверьте список получателей
4. Нажмите **🚀 Отправить все**

## 🎨 Дизайн сертификатов

### Командный сертификат
- **Цвет**: Синий (#2563EB) - медицинская тематика
- **Особенности**:
  - Цветовое кодирование мест (золото/серебро/бронза)
  - Отображение итогового балла
  - Двойная декоративная рамка
  - Медицинский символ ⚕
- **Формат**: A4 landscape (альбомная ориентация)

### Именной сертификат
- **Цвет**: Фиолетовый (#7C3AED) - индивидуальность
- **Особенности**:
  - Крупное отображение имени участника
  - Информация о команде
  - Блок специальных наград (если есть)
  - Персонализированный текст достижений
- **Формат**: A4 landscape

### Email шаблон
- Адаптивный дизайн
- Поддержка всех email-клиентов
- Эмодзи для визуальной привлекательности
- Краткое поздравление и инструкции

## 🔧 API Endpoints

### POST `/api/certificates/generate`
Генерация PDF сертификата

**Запрос**:
```json
{
  "type": "team" | "individual",
  "teamId": "team-uuid",
  "participantName": "Иванов Иван Иванович", // для individual
  "specialAward": "Лучшие практические навыки" // опционально
}
```

**Ответ**: PDF файл

### POST `/api/certificates/send`
Отправка одного сертификата по email

**Запрос**:
```json
{
  "type": "team" | "individual",
  "teamId": "team-uuid",
  "participantName": "Иванов Иван Иванович", // для individual
  "participantEmail": "participant@example.com",
  "specialAward": "Лучшие практические навыки" // опционально
}
```

**Ответ**:
```json
{
  "success": true,
  "message": "Сертификат успешно отправлен",
  "emailId": "email-id-from-resend"
}
```

### PUT `/api/certificates/send`
Массовая отправка сертификатов

**Запрос**:
```json
{
  "recipients": [
    {
      "type": "team",
      "teamId": "team-uuid",
      "participantEmail": "team@example.com"
    },
    {
      "type": "individual",
      "teamId": "team-uuid",
      "participantName": "Петров Петр Петрович",
      "participantEmail": "participant@example.com",
      "specialAward": "Специальный приз жюри"
    }
  ]
}
```

**Ответ**:
```json
{
  "success": true,
  "message": "Отправлено 2 из 2 сертификатов",
  "results": [...],
  "errors": [...]
}
```

## 🎓 Автоматическая система оценки

Система автоматически:
1. Рассчитывает общий балл команды (сумма всех конкурсов)
2. Определяет место команды (1, 2, 3 или участник)
3. Выбирает цветовую схему для места
4. Генерирует уникальный номер сертификата (формат: `OG-YYYYMM-XXXX`)
5. Добавляет текст достижений по месту

## 📊 Интеграции с Vercel

### Используемые сервисы (все БЕСПЛАТНЫЕ):

1. **Vercel KV (Redis)** ✅ Уже настроено
   - Хранение данных команд и результатов
   - Бесплатно: 256 MB, 10,000 команд/день

2. **Resend** (рекомендуется)
   - Отправка email
   - Бесплатно: 3,000 email/месяц

3. **Vercel Blob Storage** (опционально)
   - Долгосрочное хранение сертификатов
   - Бесплатно: 500 MB

### Добавление Vercel Blob (если нужно):

```bash
npm install @vercel/blob
```

В Vercel Dashboard:
1. Storage → Create Database → Blob
2. Скопируйте переменные окружения
3. Добавьте в проект

## 🔐 Безопасность

### Рекомендации:
1. ✅ Защитите `/admin/*` маршруты авторизацией
2. ✅ Валидируйте email адреса
3. ✅ Ограничьте количество запросов (rate limiting)
4. ✅ Не храните API ключи в коде
5. ✅ Используйте переменные окружения

### Пример добавления простой авторизации:

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  if (request.nextUrl.pathname.startsWith('/admin')) {
    const authHeader = request.headers.get('authorization');
    // Проверьте авторизацию
  }
}
```

## 🐛 Устранение неполадок

### Ошибка: "Cannot find module '@react-pdf/renderer'"
```bash
cd medical-competition
npm install @react-pdf/renderer
```

### Ошибка: "Resend API key not configured"
Проверьте `.env.local`:
```bash
RESEND_API_KEY=re_xxxxxxxxxx
```

### Ошибка: "Failed to send email"
1. Проверьте API key в Resend dashboard
2. Проверьте лимиты (100 email/день на бесплатном плане)
3. Для production - верифицируйте домен

### Сертификат не отображает кириллицу
- Шрифт Roboto загружается с CDN автоматически
- Проверьте интернет-соединение
- Проверьте, что используется правильная кодировка

### Медленная генерация PDF
- Нормально для первого запроса (загрузка шрифтов)
- Последующие запросы работают быстрее
- Рассмотрите кэширование для production

## 📈 Мониторинг

### В Resend Dashboard можете отслеживать:
- Количество отправленных email
- Статусы доставки
- Открытия писем
- Ошибки отправки

### В Vercel Dashboard:
- Логи функций
- Использование ресурсов
- Ошибки выполнения

## 🎉 Готово!

Система полностью настроена и готова к использованию. 

**Тестирование**:
1. Создайте тестовую команду
2. Добавьте результаты конкурсов
3. Сгенерируйте тестовый сертификат
4. Отправьте на свой email

**Production**:
1. Верифицируйте домен в Resend
2. Обновите `EMAIL_FROM`
3. Добавьте авторизацию для админ-панели
4. Настройте rate limiting

---

## 📞 Поддержка

При возникновении вопросов:
1. Проверьте логи в Vercel Dashboard
2. Проверьте статус в Resend Dashboard
3. Проверьте переменные окружения

**Важно**: Система разработана с учетом требований качественного образования - не формальный подход, а реальная помощь в организации олимпиады! 🎓